// Reset to default's
// ------------------
exec default.cfg
// Weapons Only Switching
// ----------------------
alias weaponSet "secondary"
alias primary "slot1; alias weaponSet secondary"
alias secondary "slot2; alias weaponSet primary"
bind "MWHEELUP" "weaponSet"
bind "MWHEELDOWN" "weaponSet"
//-aim                                                 // This resets the sensitivity.
//alias "aim"    "+aim"
//alias "+aim"   "sensitivity 0.5; alias aim -aim"     // Sensitivity when scoped.
//alias "-aim"   "sensitivity 1.0; alias aim +aim"     // Sensitivity when non-scoped.
//bind  "space"  "+jump"                         // If the sensitivity messes up, hit SPACE.
bind  "mouse2" "+attack2"    // You can change mouse2 to whatever you use for scoping.

echo ]]CLASS-Sniper